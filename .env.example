# Base URL of the Django API. Leave empty to use same-origin `/api`
# (works with the local Vite proxy and with nginx in Docker Compose).
# For Vercel, set this to your deployed backend, e.g. https://api.example.com/api
VITE_API_URL=
