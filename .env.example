# Base URL of the Django API. Leave empty to use same-origin `/api`
# (works with the local Vite dev proxy and with the edge nginx on EC2).
# Only set this if the API is hosted on a different origin.
VITE_API_URL=
